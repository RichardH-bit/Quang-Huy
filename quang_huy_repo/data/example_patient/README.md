Place optional processed 3D NumPy arrays here using filenames:
T00.npy, T10.npy, ..., T90.npy

Each file should contain a 3D array aligned to the same coordinate space.
If these files are absent, Step 1 will generate a synthetic demonstration dataset.
